title: Plugins
slug: plugins
hero:
  heading: plugins
  subheading: every subsystem in freddie is a plugin against one universal contract
  accent: pi for headless, gui for the dashboard, both for full-stack.
  body: >-
    Auto-generated reference of all 121 plugins discovered by bootHost(), registering 92 tools in total.
    Regenerate with `node scripts/gen-plugins-doc.mjs`.
  badges:
    - { label: "121", desc: in-tree plugins }
    - { label: "92", desc: registered tools }
    - { label: "1 contract", desc: pi + gui }
    - { label: surface guard, desc: enforced at load }
  ctas:
    - { label: contract source, href: "https://github.com/AnEntrypoint/freddie/blob/master/src/host/contract.js" }
    - { label: host source, href: "https://github.com/AnEntrypoint/freddie/blob/master/src/host/host.js" }
sections:
  - id: tool
    name: tools (27)
    lede: "agent-callable tools registered via pi.tools.register."
    features:
      - name: "ask_user"
        desc: "tool-ask_user"
      - name: "bash"
        desc: "Run a shell command. Returns stdout/stderr/exitCode."
      - name: "budget_config"
        desc: "Read/write per-session token budget limits."
      - name: "checkpoint"
        desc: "Manage conversation checkpoints. Checkpoints are automatic snapshots of the full conversation created on every user message. Use this tool to inspect, revert, or diff between checkpoints."
      - name: "clarify"
        desc: "Pose a clarifying question to the user before proceeding. Returns the user response. In non-interactive contexts, returns { error } so the agent must proceed with stated assumption."
      - name: "code_execution"
        desc: "Execute a code snippet in a chosen runner (python, node, deno, ruby, bash). Returns stdout/stderr/exitCode."
      - name: "credential-files"
        desc: "tool-credential-files"
      - name: "debug_helpers"
        desc: "Inspect any registered /debug subsystem."
      - name: "delegate"
        desc: "Spawn a sub-agent to handle a focused task. By default runs synchronously and returns the sub-agent's final result. Set run_in_background:true to instead get an agent_id back immediately while the subagent keeps working -- poll it with the subagent_status tool (action:'get', that agent_id) to see when it's done and read its result, then call delegate again with resume:<agent_id> to continue that subagent's work with new instructions."
      - name: "env_passthrough"
        desc: "Compute the env-var subset that should be passed through to spawned shells."
      - name: "image-gen"
        desc: "tool-image-gen"
      - name: "managed_tool_gateway"
        desc: "Proxy: dispatch any registered tool by name with arguments. Used for tool-level audit and policy interception."
      - name: "memory"
        desc: "Add/search/list/forget long-term memory, backed by gm rs-learn (semantic vector recall). add embeds a fact; search returns score-ranked semantic hits; list surfaces relevant memories; forget prunes by key. Always scoped to the active project's own namespace."
      - name: "mixture_of_agents"
        desc: "Run the same prompt through N sub-agents (different models or seeds), then synthesize the results. Reduces variance."
      - name: "neutts_synth"
        desc: "Local NeuTTS synth (alternate TTS backend)."
        benefit: "requires: NEUTTS_URL"
      - name: "osv_check"
        desc: "Query osv.dev for known vulnerabilities. Pass either {package, version, ecosystem} or {commit_sha, repo}."
      - name: "path_security"
        desc: "Check whether a path is allowed (no /etc/passwd, no .ssh/, etc)."
      - name: "process_registry"
        desc: "List/kill spawned background processes tracked by freddie."
      - name: "rl_training"
        desc: "Kick off an RL rollout (Atropos integration)."
        benefit: "requires: ATROPOS_URL"
      - name: "send_message"
        desc: "Send a message to a recipient on the named platform. Uses the gateway adapter; requires the platform credentials."
      - name: "subagent_status"
        desc: "Check on subagents spawned via delegate's run_in_background:true, without resuming them (scoped to subagents YOU spawned in this session). action:'list' returns every tracked subagent's status (running/completed/error/timed_out) + description, for deciding what to check next. action:'get' with agent_id returns one subagent's full record, including its result/error once status is no longer 'running'. Read-only -- to actually continue a subagent's work once you've seen it's done (or want to give it more direction), call delegate again with resume:<agent_id>, not this tool."
      - name: "terminal"
        desc: "Open a long-lived shell session, send input lines, capture output. Actions: open, send, read, close."
      - name: "tirith_security"
        desc: "Evaluate a candidate action against ~/.freddie/policy.json. Returns allow|deny|ask."
      - name: "todo"
        desc: "Manage per-session todos. Actions: add, list, update, complete, delete."
      - name: "tool_backend_helpers"
        desc: "Helper meta-tool: describeTools(filter), shapeArgs(schema, args)."
      - name: "tool_output_limits"
        desc: "Apply the configured output truncation cap to a string."
      - name: "wait"
        desc: "Pause for a fixed duration before continuing. Use when a real external process needs time to settle (a build, a deploy, a rate-limit cooldown) and there is nothing to poll."
  - id: platform
    name: platform adapters (13)
    lede: "gateway adapters exposing an EventEmitter-based platform class."
    features:
      - name: "platform-api-server"
        desc: "platform-api-server"
      - name: "platform-dingtalk"
        desc: "platform-dingtalk"
      - name: "platform-discord"
        desc: "platform-discord"
      - name: "platform-feishu"
        desc: "platform-feishu"
      - name: "platform-homeassistant"
        desc: "platform-homeassistant"
      - name: "platform-matrix"
        desc: "platform-matrix"
      - name: "platform-providers"
        desc: "platform-providers"
      - name: "platform-signal"
        desc: "platform-signal"
      - name: "platform-slack"
        desc: "platform-slack"
      - name: "platform-telegram"
        desc: "platform-telegram"
      - name: "platform-wecom"
        desc: "platform-wecom"
      - name: "platform-weixin"
        desc: "platform-weixin"
      - name: "platform-whatsapp"
        desc: "platform-whatsapp"
  - id: memory
    name: memory providers (2)
    lede: "memory providers exposing syncTurn + prefetch."
    features:
      - name: "memory-providers"
        desc: "memory-providers"
      - name: "memory-review"
        desc: "memory-review"
  - id: gui
    name: gui / dashboard (30)
    lede: "dashboard routes and pages under plugins/gui-*."
    features:
      - name: "gui-acptoapi-chains"
        desc: "gui-acptoapi-chains"
      - name: "gui-agents"
        desc: "gui-agents"
      - name: "gui-auth"
        desc: "gui-auth"
      - name: "gui-batch"
        desc: "gui-batch"
      - name: "gui-chat"
        desc: "gui-chat"
      - name: "gui-config"
        desc: "gui-config"
      - name: "gui-cron"
        desc: "gui-cron"
      - name: "gui-env"
        desc: "gui-env"
      - name: "gui-feedback"
        desc: "gui-feedback"
      - name: "gui-files"
        desc: "gui-files"
      - name: "gui-gateway"
        desc: "gui-gateway"
      - name: "gui-git"
        desc: "gui-git"
      - name: "gui-hooks"
        desc: "gui-hooks"
      - name: "gui-llm-passthrough"
        desc: "gui-llm-passthrough"
      - name: "gui-machines"
        desc: "gui-machines"
      - name: "gui-mcp"
        desc: "gui-mcp"
      - name: "gui-models-discover"
        desc: "gui-models-discover"
      - name: "gui-notifications"
        desc: "gui-notifications"
      - name: "gui-plugin-graph"
        desc: "gui-plugin-graph"
      - name: "gui-plugins-list"
        desc: "gui-plugins-list"
      - name: "gui-profiles-commands-health"
        desc: "gui-profiles-commands-health"
      - name: "gui-projects"
        desc: "gui-projects"
      - name: "gui-sessions"
        desc: "gui-sessions"
      - name: "gui-skills"
        desc: "gui-skills"
      - name: "gui-subagents"
        desc: "gui-subagents"
      - name: "gui-terminal"
        desc: "gui-terminal"
      - name: "gui-tools"
        desc: "gui-tools"
      - name: "gui-voice"
        desc: "gui-voice"
      - name: "gui-workspace-files"
        desc: "gui-workspace-files"
      - name: "gui-worktree"
        desc: "gui-worktree"
  - id: core
    name: core (6)
    lede: "core subsystem plugins (cli, cron, commands, skills, ...)."
    features:
      - name: "core-agent-machine"
        desc: "core-agent-machine"
      - name: "core-cli"
        desc: "core-cli"
      - name: "core-commands"
        desc: "core-commands"
      - name: "core-compressor"
        desc: "core-compressor"
      - name: "core-context-engine"
        desc: "core-context-engine"
      - name: "core-cron"
        desc: "core-cron"
  - id: other
    name: other (43)
    lede: "uncategorized plugins."
    features:
      - name: "achievements"
        desc: "achievements"
      - name: "agent-swarm"
        desc: "agent-swarm"
      - name: "checkpoint"
        desc: "Manage conversation checkpoints. Checkpoints are automatic snapshots of the full conversation created on every user message. Use this tool to inspect, revert, or diff between checkpoints."
      - name: "confirmation"
        desc: "confirmation"
      - name: "context-engine"
        desc: "context-engine"
      - name: "cron"
        desc: "cron"
      - name: "debug-i18n"
        desc: "debug-i18n"
      - name: "debug-naming"
        desc: "debug-naming"
      - name: "debug-perf"
        desc: "debug-perf"
      - name: "debug-plugin-failures"
        desc: "debug-plugin-failures"
      - name: "debug-profiling"
        desc: "debug-profiling"
      - name: "debug-repl"
        desc: "debug-repl"
      - name: "disk-cleanup"
        desc: "disk-cleanup"
      - name: "dmail"
        desc: "dmail"
      - name: "doctor-deep"
        desc: "doctor-deep"
      - name: "document-extraction"
        desc: "document-extraction"
      - name: "feature-flags"
        desc: "feature-flags"
      - name: "files"
        desc: "files"
      - name: "glob"
        desc: "Find files matching a glob pattern. Uses ripgrep under the hood. Matches files only, not directories. Respects .gitignore by default. Results sorted by modification time, most recent first, capped at 100."
      - name: "goal"
        desc: "goal"
      - name: "google-meet"
        desc: "google-meet"
      - name: "init-agent"
        desc: "init-agent"
      - name: "mcp"
        desc: "mcp"
      - name: "media"
        desc: "media"
      - name: "observability"
        desc: "observability"
      - name: "plan-mode"
        desc: "plan-mode"
      - name: "plugin-validate"
        desc: "plugin-validate"
      - name: "plugin-validate-bulk"
        desc: "plugin-validate-bulk"
      - name: "read-media-file"
        desc: "read-media-file"
      - name: "session-io"
        desc: "session-io"
      - name: "session-replay"
        desc: "session-replay"
      - name: "sessions"
        desc: "sessions"
      - name: "skills"
        desc: "skills"
      - name: "skills-guard"
        desc: "skills-guard"
      - name: "skills-hub"
        desc: "skills-hub"
      - name: "spoint-editor"
        desc: "spoint-editor"
      - name: "spotify"
        desc: "Spotify playback control (Web API)."
        benefit: "requires: SPOTIFY_ACCESS_TOKEN"
      - name: "task"
        desc: "List or inspect background tasks. Without a task_id, returns all active background tasks. With a task_id, returns full details of that specific task. Use subcommand \"list\" with all=true to show all tasks including completed, \"clean\" to remove completed/failed tasks, \"reconcile\" to detect stale tasks, or \"cleanup\" to remove old terminal tasks."
      - name: "think"
        desc: "Record a structured thought for reasoning. This is a no-op logging tool — it simply records the thought and returns ok."
      - name: "todo"
        desc: "Manage per-session todos. Actions: add, list, update, complete, delete."
      - name: "web"
        desc: "web"
      - name: "wire"
        desc: "wire"
      - name: "workflow-templates"
        desc: "workflow-templates"
