# Plugins

Auto-generated by `scripts/gen-plugins-doc.mjs`. Do not edit by hand — re-run the script instead.

121 plugins discovered by `bootHost()`, registering 92 tools.

## Tools (27)

| Name | Toolset | Description | Requires env | Surfaces |
| --- | --- | --- | --- | --- |
| `ask_user` | core |  |  | pi |
| `bash` | core | Run a shell command. Returns stdout/stderr/exitCode. |  | pi |
| `budget_config` | core | Read/write per-session token budget limits. |  | pi |
| `checkpoint` | core | Manage conversation checkpoints. Checkpoints are automatic snapshots of the full conversation created on every user message. Use this tool to inspect, revert, or diff between checkpoints. |  | pi |
| `clarify` | core | Pose a clarifying question to the user before proceeding. Returns the user response. In non-interactive contexts, returns { error } so the agent must proceed with stated assumption. |  | pi |
| `code_execution` | core | Execute a code snippet in a chosen runner (python, node, deno, ruby, bash). Returns stdout/stderr/exitCode. |  | pi |
| `credential-files` | core |  |  | pi |
| `debug_helpers` | core | Inspect any registered /debug subsystem. |  | pi |
| `delegate` | core | Spawn a sub-agent to handle a focused task. By default runs synchronously and returns the sub-agent's final result. Set run_in_background:true to instead get an agent_id back immediately while the subagent keeps working -- poll it with the subagent_status tool (action:'get', that agent_id) to see when it's done and read its result, then call delegate again with resume:<agent_id> to continue that subagent's work with new instructions. |  | pi |
| `env_passthrough` | core | Compute the env-var subset that should be passed through to spawned shells. |  | pi |
| `image-gen` | core |  |  | pi |
| `managed_tool_gateway` | core | Proxy: dispatch any registered tool by name with arguments. Used for tool-level audit and policy interception. |  | pi |
| `memory` | core | Add/search/list/forget long-term memory, backed by gm rs-learn (semantic vector recall). add embeds a fact; search returns score-ranked semantic hits; list surfaces relevant memories; forget prunes by key. Always scoped to the active project's own namespace. |  | pi |
| `mixture_of_agents` | core | Run the same prompt through N sub-agents (different models or seeds), then synthesize the results. Reduces variance. |  | pi |
| `neutts_synth` | creative | Local NeuTTS synth (alternate TTS backend). | NEUTTS_URL | pi |
| `osv_check` | core | Query osv.dev for known vulnerabilities. Pass either {package, version, ecosystem} or {commit_sha, repo}. |  | pi |
| `path_security` | core | Check whether a path is allowed (no /etc/passwd, no .ssh/, etc). |  | pi |
| `process_registry` | core | List/kill spawned background processes tracked by freddie. |  | pi |
| `rl_training` | core | Kick off an RL rollout (Atropos integration). | ATROPOS_URL | pi |
| `send_message` | core | Send a message to a recipient on the named platform. Uses the gateway adapter; requires the platform credentials. |  | pi |
| `subagent_status` | core | Check on subagents spawned via delegate's run_in_background:true, without resuming them (scoped to subagents YOU spawned in this session). action:'list' returns every tracked subagent's status (running/completed/error/timed_out) + description, for deciding what to check next. action:'get' with agent_id returns one subagent's full record, including its result/error once status is no longer 'running'. Read-only -- to actually continue a subagent's work once you've seen it's done (or want to give it more direction), call delegate again with resume:<agent_id>, not this tool. |  | pi |
| `terminal` | core | Open a long-lived shell session, send input lines, capture output. Actions: open, send, read, close. |  | pi |
| `tirith_security` | core | Evaluate a candidate action against ~/.freddie/policy.json. Returns allow\|deny\|ask. |  | pi |
| `todo` | core | Manage per-session todos. Actions: add, list, update, complete, delete. |  | pi |
| `tool_backend_helpers` | core | Helper meta-tool: describeTools(filter), shapeArgs(schema, args). |  | pi |
| `tool_output_limits` | core | Apply the configured output truncation cap to a string. |  | pi |
| `wait` | core | Pause for a fixed duration before continuing. Use when a real external process needs time to settle (a build, a deploy, a rate-limit cooldown) and there is nothing to poll. |  | pi |

## Platform adapters (13)

| Name | Toolset | Description | Requires env | Surfaces |
| --- | --- | --- | --- | --- |
| `platform-api-server` |  |  |  | pi |
| `platform-dingtalk` |  |  |  | pi |
| `platform-discord` |  |  |  | pi |
| `platform-feishu` |  |  |  | pi |
| `platform-homeassistant` |  |  |  | pi |
| `platform-matrix` |  |  |  | pi |
| `platform-providers` |  |  |  | pi |
| `platform-signal` |  |  |  | pi |
| `platform-slack` |  |  |  | pi |
| `platform-telegram` |  |  |  | pi |
| `platform-wecom` |  |  |  | pi |
| `platform-weixin` |  |  |  | pi |
| `platform-whatsapp` |  |  |  | pi |

## Memory providers (2)

| Name | Toolset | Description | Requires env | Surfaces |
| --- | --- | --- | --- | --- |
| `memory-providers` |  |  |  | pi |
| `memory-review` |  |  |  | pi |

## GUI / dashboard (30)

| Name | Toolset | Description | Requires env | Surfaces |
| --- | --- | --- | --- | --- |
| `gui-acptoapi-chains` |  |  |  | gui |
| `gui-agents` |  |  |  | gui |
| `gui-auth` |  |  |  | gui |
| `gui-batch` |  |  |  | gui |
| `gui-chat` |  |  |  | gui |
| `gui-config` |  |  |  | gui |
| `gui-cron` |  |  |  | gui |
| `gui-env` |  |  |  | gui |
| `gui-feedback` |  |  |  | gui |
| `gui-files` |  |  |  | gui |
| `gui-gateway` |  |  |  | gui |
| `gui-git` |  |  |  | gui |
| `gui-hooks` |  |  |  | gui |
| `gui-llm-passthrough` |  |  |  | gui |
| `gui-machines` |  |  |  | gui |
| `gui-mcp` |  |  |  | gui |
| `gui-models-discover` |  |  |  | gui |
| `gui-notifications` |  |  |  | gui |
| `gui-plugin-graph` |  |  |  | gui |
| `gui-plugins-list` |  |  |  | gui |
| `gui-profiles-commands-health` |  |  |  | gui |
| `gui-projects` |  |  |  | gui |
| `gui-sessions` |  |  |  | gui |
| `gui-skills` |  |  |  | gui |
| `gui-subagents` |  |  |  | gui |
| `gui-terminal` |  |  |  | gui |
| `gui-tools` |  |  |  | gui |
| `gui-voice` |  |  |  | gui |
| `gui-workspace-files` |  |  |  | gui |
| `gui-worktree` |  |  |  | gui |

## Core (6)

| Name | Toolset | Description | Requires env | Surfaces |
| --- | --- | --- | --- | --- |
| `core-agent-machine` |  |  |  | pi |
| `core-cli` |  |  |  | pi |
| `core-commands` |  |  |  | pi |
| `core-compressor` |  |  |  | pi |
| `core-context-engine` |  |  |  | pi |
| `core-cron` |  |  |  | pi |

## Other (43)

| Name | Toolset | Description | Requires env | Surfaces |
| --- | --- | --- | --- | --- |
| `achievements` |  |  |  | pi |
| `agent-swarm` |  |  |  | pi |
| `checkpoint` | core | Manage conversation checkpoints. Checkpoints are automatic snapshots of the full conversation created on every user message. Use this tool to inspect, revert, or diff between checkpoints. |  | pi |
| `confirmation` |  |  |  | pi |
| `context-engine` |  |  |  | pi |
| `cron` |  |  |  | pi |
| `debug-i18n` |  |  |  | pi |
| `debug-naming` |  |  |  | pi |
| `debug-perf` |  |  |  | pi |
| `debug-plugin-failures` |  |  |  | pi |
| `debug-profiling` |  |  |  | pi |
| `debug-repl` |  |  |  | pi |
| `disk-cleanup` |  |  |  | pi |
| `dmail` |  |  |  | pi |
| `doctor-deep` |  |  |  | pi |
| `document-extraction` |  |  |  | pi |
| `feature-flags` |  |  |  | pi |
| `files` |  |  |  | pi |
| `glob` | core | Find files matching a glob pattern. Uses ripgrep under the hood. Matches files only, not directories. Respects .gitignore by default. Results sorted by modification time, most recent first, capped at 100. |  | pi |
| `goal` |  |  |  | pi |
| `google-meet` |  |  |  | pi |
| `init-agent` |  |  |  | pi |
| `mcp` |  |  |  | pi |
| `media` |  |  |  | pi |
| `observability` |  |  |  | both |
| `plan-mode` |  |  |  | pi |
| `plugin-validate` |  |  |  | pi |
| `plugin-validate-bulk` |  |  |  | pi |
| `read-media-file` |  |  |  | pi |
| `session-io` |  |  |  | pi |
| `session-replay` |  |  |  | pi |
| `sessions` |  |  |  | pi |
| `skills` |  |  |  | pi |
| `skills-guard` |  |  |  | pi |
| `skills-hub` |  |  |  | pi |
| `spoint-editor` |  |  |  | pi |
| `spotify` | core | Spotify playback control (Web API). | SPOTIFY_ACCESS_TOKEN | pi |
| `task` | core | List or inspect background tasks. Without a task_id, returns all active background tasks. With a task_id, returns full details of that specific task. Use subcommand "list" with all=true to show all tasks including completed, "clean" to remove completed/failed tasks, "reconcile" to detect stale tasks, or "cleanup" to remove old terminal tasks. |  | pi |
| `think` | core | Record a structured thought for reasoning. This is a no-op logging tool — it simply records the thought and returns ok. |  | pi |
| `todo` | core | Manage per-session todos. Actions: add, list, update, complete, delete. |  | pi |
| `web` |  |  |  | pi |
| `wire` |  |  |  | pi |
| `workflow-templates` |  |  |  | pi |

